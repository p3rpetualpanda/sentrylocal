{
    "eval": ["Use of eval() is dangerous and should be avoided."],
    "secrets": ["API_KEY", "PASSWORD"],
    "unsafe_queries": [
        "SELECT * FROM users WHERE username =",
        "DELETE FROM users WHERE id="
    ],
    "os_system": ["Use of os.system() is dangerous and can lead to command injection."],
    "insecure_imports": [
        {"module": "subprocess", "reason": "Consider using subprocess.run instead."},
        {"module": "eval", "reason": "Avoid using eval for untrusted input."}
    ]
}